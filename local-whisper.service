[Unit]
Description=Local Whisper hold-to-dictate
After=graphical-session.target sound.target

[Service]
Type=simple
ExecStart=/usr/bin/env bash -lc 'cd "$HOME/Local Whisper" && exec ./run_dictation.sh'
Restart=on-failure
RestartSec=3

[Install]
WantedBy=default.target
